'use strict';
let hasDriversLicense = false;
const passTest = true;

if (passTest) hasDriversLicense = true;
if (hasDriversLicense) console.log("I can drive")
 //using strict mode shows the reserved word error incase if we use the reserved words
 //eg const private = 34; this wont work because the private is a reserved word;

